<p>
  <strong>Cliente:</strong>
  <%= @budget.client.name %>
</p>

<p>
  <strong>Dirección:</strong>
  <%= @budget.address %>
</p>


<%= render 'line_items/index', line_items: @line_items, line_itemable: @budget %>

<%= link_to 'Enviar Estimado', email_budget_path(@budget), class: 'btn btn-primary' %>