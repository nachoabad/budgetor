<div class="d-flex justify-content-between">
  <h3>Mis Presupuestos</h3>
  <%= link_to 'Nuevo Presupuesto', new_budget_path %>
</div>

<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Cliente</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @budgets.each do |budget| %>
        <tr>
          <td><%= budget.client.name %></td>
          <td><%= link_to 'Ver', client %></td>
          <td><%= link_to 'Editar', edit_budget_path(client) %></td>
          <td><%= link_to 'Borrar', budget, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
